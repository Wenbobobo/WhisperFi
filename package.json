{
  "name": "private-defi",
  "version": "1.0.0",
  "main": "hardhat.config.js",
  "directories": {
    "doc": "docs",
    "test": "test"
  },
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "compile-circuits": "cross-env NODE_PATH=node_modules/ circom circuits/deposit.circom --r1cs --wasm --output circuits/build/deposit && cross-env NODE_PATH=node_modules/ circom circuits/withdraw.circom --r1cs --wasm --output circuits/build/withdraw && cross-env NODE_PATH=node_modules/ circom circuits/trade.circom --r1cs --wasm --output circuits/build/trade"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "@playwright/test": "^1.54.1",
    "circom": "^0.5.46",
    "circomlibjs": "^0.1.7",
    "hardhat": "^2.25.0",
    "hardhat-preprocessor": "^0.1.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "@openzeppelin/contracts-upgradeable": "^5.3.0",
    "axios": "^1.10.0",
    "circomlib": "^2.0.5",
    "cross-env": "^7.0.3",
    "erc4337": "^0.0.1-security",
    "ethers": "^6.15.0",
    "fixed-merkle-tree": "^0.7.3",
    "pino-pretty": "^13.0.0",
    "snarkjs": "^0.7.5"
  }
}
